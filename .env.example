# PostHog project key (public, non-secret)
# Set in GitHub repo Settings > Variables (not Secrets) for CI builds.
VITE_POSTHOG_KEY=
# PostHog API host — defaults to https://us.i.posthog.com if omitted
VITE_POSTHOG_HOST=
